import { ArrayTohighPipe } from './array-tohigh.pipe';

describe('ArrayTohighPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayTohighPipe();
    expect(pipe).toBeTruthy();
  });
});
